# Makefile for rainbow build

all:
	@echo "------------------------------------------"
	@echo "make build: create dist folder"
	@echo "------------------------------------------"

build:
	npm run build
